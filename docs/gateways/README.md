<div dir="rtl">

# راهنمای اتصال درگاه‌ها — django-iranian-payment

این پوشه برای هر درگاه یک راهنمای کامل و خودکفا دارد: از نصب تا verify، قدم‌به‌قدم،
برای **هر دو حالت** مدیریت دیتابیس. هر فایل مستقل است؛ لازم نیست فایل دیگری بخوانی.

## دو حالت مدیریت دیتابیس

هر درگاه را می‌توان به دو شکل در پروژه‌ی Django استفاده کرد:

- **حالت ۱ — پکیج دیتابیس را مدیریت می‌کند:** اپ `contrib.django` را به
  `INSTALLED_APPS` اضافه می‌کنی، `migrate` می‌زنی، و از سرویس‌های آماده
  (`start_payment`/`verify_payment`) + مدل `Payment` + view و url داخلی استفاده
  می‌کنی. تقریباً هیچ منطقی نمی‌نویسی (جعبه‌سیاه).
- **حالت ۲ — خودت دیتابیس را مدیریت می‌کنی:** فقط از هسته‌ی بدون state
  (`get_gateway` + `PaymentRequest` + `initiate`/`verify`) استفاده می‌کنی و رکورد
  پرداخت را در مدل خودت ذخیره می‌کنی. کنترل کامل روی schema و جریان.

> قانون طلایی هر دو حالت: **واحد بانک همیشه ریال است** و **`amount_to_send` که در
> `initiate` برگشت، همان باید در `verify` برود** (نه مبلغ پایه‌ی سفارش). در حالت ۱
> پکیج این را خودکار با `amount_sent` انجام می‌دهد؛ در حالت ۲ خودت باید رعایت کنی.

## واحد پول (ریال یا تومان)

بانک همیشه ریال می‌گیرد، ولی واحدی که **خودت ورودی می‌دهی** را می‌توانی به‌صورت
سراسری انتخاب کنی:

<div dir="ltr">

```python
IRANIAN_PAYMENT = {
    "currency": "toman",   # "rial" (پیش‌فرض) یا "toman"
    "gateways": {...},
}
# با toman: amount=15_000 یعنی ۱۵۰۰۰ تومان → بانک ۱۵۰۰۰۰ ریال می‌گیرد.
```
</div>

- پیش‌فرض `"rial"` (سازگار با قبل). تبدیل **۱ تومان = ۱۰ ریال** فقط یک‌بار در ابتدا.
- مبالغ ذخیره‌شده در مدل و مقادیر بازگشتی verify **همیشه ریال‌اند**؛ `currency` فقط
  واحد *ورودی* را تعیین می‌کند. برای نمایش تومان به کاربر خودت بر ۱۰ تقسیم کن.
- در حالت ۲ (هسته)، واحد را روی خود درخواست بده:
  `PaymentRequest(amount=15_000, currency="toman", ...)`؛ یا مقدار سراسری را با
  `get_default_currency()` بخوان. در نمونه‌های هر راهنما `amount` به ریال است؛ اگر
  `currency="toman"` گذاشتی، همان عددها را تومانی تفسیر کن.

## فهرست درگاه‌ها

| درگاه | وضعیت | sandbox مجزا؟ | وابستگی اختیاری | راهنما |
|-------|-------|---------------|------------------|--------|
| زرین‌پال | ✅ registry عمومی (sandbox تست‌شده) | بله (`sandbox.zarinpal.com`) | — | [zarinpal.md](zarinpal.md) |
| زیبال | ✅ registry عمومی (sandbox تست‌شده) | با `merchant="zibal"` | — | [zibal.md](zibal.md) |
| ملت | ✅ registry عمومی (**تراکنش live تست‌شده**) | بله | `[soap]` | [mellat.md](mellat.md) |
| سامان (SEP) | ⚠️ تجربی (تست‌نشده) | ندارد | — | [saman.md](saman.md) |
| ایران‌کیش | ⚠️ تجربی (تست‌نشده) | ندارد | `[irankish]` | [irankish.md](irankish.md) |
| نکست‌پی | ⚠️ تجربی (تست‌نشده) | ندارد | — | [nextpay.md](nextpay.md) |
| سداد | ⚠️ تجربی (تست‌نشده) | ندارد | `[sadad]` | [sadad.md](sadad.md) |
| دیجی‌پی | ⚠️ تجربی (تست‌نشده) | بله (`uat.mydigipay.info`) | — | [digipay.md](digipay.md) |

> ⚠️ **صداقت:** درگاه‌های «تجربی» پیاده‌سازی کامل از مستند رسمی دارند و منطقشان با
> تست خودکار پوشش داده شده، ولی **هنوز با ترمینال/sandbox واقعی تأیید نشده‌اند**.
> طبق قانون طلایی پروژه در registry عمومی نیستند و برای استفاده باید صریحاً register
> شوند (در هر راهنما توضیح داده شده). شکل دقیق فیلدهای callback آن‌ها از مستندات است.

## sandbox مجزای هر درگاه

از نسخه‌ی فعلی، sandbox هر درگاه جداگانه تعیین می‌شود. کلید `"sandbox"` داخل config
همان درگاه بر مقدار سراسری اولویت دارد:

<div dir="ltr">

```python
IRANIAN_PAYMENT = {
    "sandbox": False,            # پیش‌فرض سراسری
    "gateways": {
        "zarinpal": {"merchant_id": "...", "sandbox": True},   # این درگاه sandbox
        "mellat":   {"terminal_id": "...", ...},               # این درگاه live (از سراسری)
    },
}
```
</div>

اولویت کامل: آرگومان `get_gateway(..., sandbox=...)` ← config درگاه ← `sandbox` سراسری ← `False`.

نکته: فقط درگاه‌هایی که URL سندباکس جدا دارند (زرین‌پال، ملت، دیجی‌پی) به این فلگ
واکنش می‌دهند. زیبال با `merchant="zibal"` و سامان/ایران‌کیش/نکست‌پی/سداد اصلاً
URL سندباکس جدا ندارند (در راهنمای هرکدام ذکر شده).

## کد نمونه‌ی اجراشدنی

کنار این راهنماها، فایل‌های `scripts/django_<slug>.py` همان کد را به‌صورت قابل
کپی/اجرا دارند، و `scripts/django_custom_db.py` الگوی عمومی حالت ۲ را برای همه‌ی
درگاه‌ها با یک مسیر کد واحد نشان می‌دهد.
</div>
